{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "resourceBaseName": {
            "value": "GhPrHelperStaging"
        },
        "bot_aadClientId": {
            "value": "{{profile.fx-resource-bot.botId}}"
        },
        "bot_aadClientSecret": {
            "value": "{{BOT_AAD_APP_SECRET}}"
        },
        "m365ClientId": {
            "value": "{{profile.fx-resource-aad-app-for-teams.clientId}}"
        },
        "m365ClientSecret": {
            "value": "{{M365_AAD_APP_SECRET}}"
        },
        "m365TenantId": {
            "value": "{{profile.fx-resource-aad-app-for-teams.tenantId}}"
        },
        "m365OauthAuthorityHost": {
            "value": "{{profile.fx-resource-aad-app-for-teams.oauthHost}}"
        },
        "bot_webAppSKU": {
            "value": "F1"
        }
    }
}